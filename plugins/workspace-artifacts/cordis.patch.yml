# workspace-artifacts bundle layer: inserts the workspace-artifacts
# row into the host composition when this bundle is added to a profile.
# The client half is discovered via package.json dsh.client + exports["./client"].
- insert:
    - id: workspace-artifacts
      name: workspace-artifacts
